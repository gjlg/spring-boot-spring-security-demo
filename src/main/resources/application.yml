spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    timeout: 10000
    host: 101.200.206.63
    port: 6379
    password: rp@ssw0rd
    database: 6
    lettuce:
      pool:
        max-active: 10
        max-wait: -1
        max-idle: 8
  application:
    name: springboot-spring-security-demo
mybatis-plus:
  mapper-locations: classpath*:/mapper-xml/**/*.xml
